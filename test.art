import ./{tasks}!

createTempDir: routine [
    |> 'echo {Creating dir}
    |> 'mkdir {temp}
]

removeTempDir: routine [
    |> 'echo {Removing dir}
    |> 'rmdir {temp}
]


task 'test .requires: [createTempDir] .defer: [removeTempDir] [
    |> 'echo {"Testing"}
]