import ./{tasks}!

createTempDir: $[][
    |> 'echo {Creating dir}
    |> 'mkdir {temp}
]

removeTempDir: $[][
    |> 'echo {Removing dir}
    |> 'rmdir {temp}
]


task 'test .requires: [createTempDir] .defer: [removeTempDir] [
    |> 'echo {"Testing"}
]